# so-chart public repository guidance

本文件适用于公开版本分支。公开仓库不得包含凭据、内部服务地址、个人配置、真实业务数据或未确认授权的资源。

## 代码与文档

- 修改 `packages/*/src` 时，同步对应包的真实示例、`demo/docs` 和 README。
- 新增或修改公开类型时，同时检查运行时默认值、归一化、序列化和实际渲染逻辑。
- 示例必须从包的 `demo/manifest.ts` 暴露，并能通过预览站访问。
- 图表实例、事件监听器、动画帧和观察器必须在卸载时释放。
- 不要手工修改 `dist`，也不要提交构建缓存。

## 公开边界

- registry 配置只能使用公共 registry 或本地环境变量，任何 token 都不能进入仓库。
- 内部 CI、部署、镜像、网络地址和审查服务不属于公开版本。
- 示例图片、字体和数据必须具备公开授权；无法确认时使用代码或 CSS 生成的占位资源。
- 公开分支与内部开发版本采用人工同步，遵守 `PUBLIC_SYNC.md`。

## 验证

按变更范围执行最小充分检查，至少在相关变更上运行 `git diff --check`。涉及包构建或预览站时，验证对应构建；涉及交互时，检查桌面和移动视口。不要未经需要运行与变更无关的全仓库检查。
